spring:
  application:
    name: DISCOVERY-SERVICE
  cloud:
    config:
      fail-fast: true
      retry:
        max-attempts: 10
        initial-interval: 1500
        multiplier: 1.5
      uri: http://localhost:8888
      username: think
      password: microservices
  profiles:
    active: dev
